"""Test-only conftest for sterna tests.

The middleware-related tests exercise the request lifecycle directly
via RequestFactory rather than a registered URL, so no probe URL is
needed here. This file exists as a placeholder for future fixtures.
"""
