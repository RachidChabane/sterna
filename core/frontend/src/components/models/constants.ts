/**
 * Shared constants for model comparison and chat functionality
 */

import { getDefaultModelParameters } from '@/config/modelParameters'

export const MAX_CHATS = 10

// Use centralized default parameters to avoid duplication
export const DEFAULT_PARAMETERS = getDefaultModelParameters()
