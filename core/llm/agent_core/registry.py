"""The catalog of tools the agent execution loop can offer a model.

Defines where a tool's handler, schema, and display metadata are
co-located and looked up by name, so the loop and its callers share
one source of truth for what tools exist.
"""
