apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# CCM + CSI for Hetzner. Applied as part of the post-Terraform
# bootstrap, before the app workload (see ../bootstrap-staging/).

resources:
  - ccm.yaml
  - csi.yaml
