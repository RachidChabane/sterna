# gVisor RuntimeClass for sandbox isolation
# gVisor is installed via cloud-init on K3s nodes
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
handler: runsc
scheduling:
  nodeSelector:
    sternaway.ai/gvisor: "true"
