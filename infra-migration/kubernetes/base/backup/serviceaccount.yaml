# =============================================================================
# Backup Service Account
# =============================================================================
# Service account for database backup jobs with minimal permissions
apiVersion: v1
kind: ServiceAccount
metadata:
  name: backup
  namespace: sterna
  labels:
    app.kubernetes.io/name: backup
    app.kubernetes.io/component: backup
